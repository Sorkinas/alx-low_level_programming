#include "holberton.h"

/**
 * print_last_digit - prints the last digit of an integer
 * @i: an integer
 *
 * Return: the value of the last digit of int i.
 */
int print_last_digit(int i)
{
	int j = i;

	if (i < 0)
		i = i * -1;
	i = i % 10;
	_putchar(i + '0');
	return (j);
}
